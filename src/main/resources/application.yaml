spring:
  application:
    name: guest-spring-security-jwt
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    url: jdbc:postgresql://postgresdb:5432/guests
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      #ddl-auto: none
    properties:
      hibernate.format_sql: true

app:
  secret-key: abc2005